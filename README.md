# TempusMail-Replica
TempusMail-Replica is a web application that allows users to generate temporary email addresses instantly. It provides a fully functional inbox where emails sent to the generated address are received in real-time.
